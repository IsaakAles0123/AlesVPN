package com.myvpn.app.ui.theme

import androidx.compose.ui.unit.dp

/** Сетка отступов 8 dp: везде одинаковые «шаги». */
object AlesSpacing {
    val screenHorizontal = 24.dp
    val screenVertical = 24.dp
    /** Между крупными блоками (заголовок — статус — слайдер — секции). */
    val section = 16.dp
    /** Внутри секции, между полем и кнопкой. */
    val item = 12.dp
    val small = 8.dp
    /** Внутри карточки лога. */
    val cardInner = 16.dp
}
