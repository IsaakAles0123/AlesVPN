package com.myvpn.app.ui.theme

import androidx.compose.ui.graphics.Color

val BackgroundDeep = Color(0xFF050508)
/** Тёплый подтон «зала» поверх глубокого фона. */
val BackgroundWarm = Color(0xFF0C0806)

val AccentGold = Color(0xFFC9A227)
val AccentGoldBright = Color(0xFFE8C547)
val AccentGoldDim = Color(0xFF7A6220)
/** Нашивка на чёрном поясе (подключено). */
val GoldPatch = Color(0xFFFFD966)

val TextPrimary = Color(0xFFF8F4EC)
val TextMuted = Color(0xFF9CA3AF)
val CardGlass = Color(0x332A2218)

/** Белый пояс (не подключено). */
val BeltWhite = Color(0xFFF5F5F5)
/** Промежуточное состояние подключения. */
val BeltConnecting = Color(0xFFB0B0B0)
/** Чёрный пояс (подключено). */
val BeltBlack = Color(0xFF1A1A1A)

val DobokFabric = Color(0xFFECECEC)
val DobokTrim = Color(0xFF252525)
